pytest
torch
numpy
pandas
requests

# If pip cannot find a compatible torch wheel on your platform,
# the PyTorch CPU wheels are available on the following index.
# Pip will consult this index to find a prebuilt CPU-only wheel.
# Example: pip install --index-url https://download.pytorch.org/whl/cpu torch
